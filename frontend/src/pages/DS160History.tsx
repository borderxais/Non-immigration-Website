import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const DS160History: React.FC = () => {
  return (
    <div>
      <Title level={2}>DS-160 历史记录</Title>
    </div>
  );
};

export default DS160History;
