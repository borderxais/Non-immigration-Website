import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const PlatformApp: React.FC = () => {
  return (
    <div>
      <Title level={2}>APP下载（即将推出）</Title>
    </div>
  );
};

export default PlatformApp;
