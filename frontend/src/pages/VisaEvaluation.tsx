import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const VisaEvaluation: React.FC = () => {
  return (
    <div>
      <Title level={2}>签证评估</Title>
    </div>
  );
};

export default VisaEvaluation;
